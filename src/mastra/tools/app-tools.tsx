// import { createTool } from "@mastra/core/tools";
// import { z } from "zod";
//
// export const appInfo = createTool({
//   id: "Get OpenMTP info",
//   inputSchema: z.object({
//     info: z.string(),
//   }),
//   description: `Fetches the openmtp info`,
//   execute: async ({ context: { info } }) => {
//
//     return { info };
//   },
// });
